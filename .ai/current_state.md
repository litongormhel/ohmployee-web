# Current State

OHMployee Web is a Next.js App Router project with a lightweight admin command shell, authentication page, and scaffold-only module routes.

## Foundation

- App Router root remains in `src/app`.
- Reusable components live in `src/components`.
- Shared library code lives in `src/lib`.
- React Query is available app-wide through the providers wrapper.
- Supabase client environment variables are documented in `.env.example`.
- Dashboard routes use a persistent admin shell with a desktop sidebar, topbar, responsive mobile navigation strip, and centered main content container.
- The web module map is centralized in `src/lib/modules.ts`.
- Dashboard routes now validate the Supabase Auth user and load the web-safe backend context through `get_web_current_user_context` in `src/components/DashboardAuthShell.tsx`.
- Unauthenticated dashboard visits redirect to `/login`.
- `/login` redirects already-authenticated visitors to `/dashboard`, and successful login lands on `/dashboard`.
- Logout is available in the sidebar and signs out before redirecting to `/login`.
- This web repo currently has no local `supabase/migrations` directory.
- The frontend now requires `public.get_web_current_user_context()` as the canonical Supabase RPC for web shell bootstrap, with an optional `security_invoker` view named `public.web_current_user_context` after the RPC is safe.
- The admin shell renders only when the RPC returns `access_status === "allowed"`.
- Backend `allowed_module_keys` drives sidebar/topbar module visibility; `module_capabilities` is kept presentation-only.
- Missing profiles, inactive/disabled accounts, unauthorized web roles, RPC failures, malformed module arrays, and malformed capability payloads render a blocked-access screen instead of the shell.
- The Shared Web UI System architecture has been defined and documented in `docs/state/web_ui_system_state.md`, auditing existing Vacancy UI patterns, specifying reusable admin primitives, defining visual system tokens, and mapping out a phased extraction roadmap.


## Module Registry

- Dashboard: `/dashboard`
- CENCOM: `/cencom`
- Vacancy: `/vacancy`
- HR Emploc: `/hr-emploc`
- Plantilla: `/plantilla`
- Approvals: `/approvals`
- User Management: `/users`
- Team Directory: `/team-directory`
- Notifications: `/notifications`
- Reports: `/reports`
- Settings: `/settings`
- More: `/more`

## Not Implemented

- Most Supabase business queries, RLS policies, and module workflows are not implemented in this web repo. Vacancy Web now calls its approved read-only RPC names.
- Backend profile/scope loading and real RBAC capability filtering are wired to the expected RPC contract, but the database implementation is outside this web repo.
- An allowed user with an empty `allowed_module_keys` array receives dashboard-only navigation; missing module/capability fields fail closed.
- Vacancy data access is now wired through `get_web_vacancy_summary(...)` and `list_web_vacancies(...)` only.
- Vacancy list row typing and rendering now use the RPC field name `position_title` rather than a legacy `position`/camelCase frontend alias.
- Vacancy Web target architecture is now documented in `docs/state/vacancy_web_state.md`: desktop admin command-center layout, KPI row, status tabs, filter/search toolbar, dense table, detail drawer, applicant placeholder, and capability-controlled action area.
- Vacancy Web is fully integrated with the detail read contract `public.get_web_vacancy_detail(p_vacancy_id uuid)` in the frontend.
- Blank module pages intentionally avoid mocked records, charts, and CRUD actions.
- The safe web current-user context RPC/view contract is documented in `docs/state/web_auth_rbac_state.md`; this repo has no local migration for it.
- Frontend code may use module capabilities only for shell display metadata. Supabase RLS/RPC remains the authority for all actions and business data.
- Vacancy Web now renders a high-fidelity desktop/admin-first read-only overlay drawer for vacancy details instead of a static selection sidebar. The dense list table is full screen width.
- Vacancy Web fetches only from the approved read RPCs. It does not render fake rows, query raw tables, include sample employee/applicant names, bypass RLS, or implement CRUD/mutations.
- `OHM2026_1077` and `OHM2026_1082` document the proposed Vacancy Web read contracts in `docs/state/vacancy_web_state.md`. The recommended backend-first list contract is `public.list_web_vacancies(...)` and `public.get_web_vacancy_summary(...)`. The recommended detail drawer contract is `public.get_web_vacancy_detail(p_vacancy_id uuid)`.
- `OHM2026_1077` and `OHM2026_1082` document the proposed Vacancy Web read contracts in `docs/state/vacancy_web_state.md`. The recommended backend-first list contract is `public.list_web_vacancies(...)` and `public.get_web_summary(...)`. The recommended detail drawer contract is `public.get_web_vacancy_detail(p_vacancy_id uuid)`.
- The Web Vacancy detail contract wraps existing Mobile semantics from `vw_vacancy_detail`, `fn_is_active_vacancy_applicant_status`, active applicant lists, and existing RBAC/scope helpers. It derives the caller from `auth.uid()`, enforces Super Admin/Head Admin broad access and scoped OM/ATL/TL/HRCO/Recruitment/Viewer reads, separates list-only vs. detail-only fields, and returns row capabilities (`can_approve`, `can_update_applicant_status`, `can_request_closure`) without exposing applicant PII (contact numbers or email addresses).
- Remaining Vacancy gaps are backend availability/verification of the scoped list, summary, and detail RPCs in the target Supabase project, followed by Phase 4 (mutations and action RPCs).
- The Shared Web UI System Phase 2 low-risk primitives (`AdminPageHeader`, `MetricCard`, `DataState`, and `StatusBadge`) are fully implemented under `src/components/shared/` and `src/components/ui/` with lightweight TypeScript typings, explicit prop mappings, and strict aesthetic adherence. The Phase 3 structural layout components (`AdminFilterBar`, `CapabilityActionBar`, and `DetailDrawer`) are also fully implemented as generic, domain-neutral containers under `src/components/shared/`.
- The Vacancy module has been fully refactored to consume the central Shared Web UI System (Phase 4): the list page uses `AdminPageHeader`, `MetricCard`, and `AdminFilterBar`, and the detail drawer uses `DetailDrawer`, `CapabilityActionBar`, and `StatusBadge` where safe. All exact read-only queries, filter query parameter bindings, active pipeline filters, pagination, access-denied RLS shields, non-PII candidate structures, and historical timelines are preserved perfectly with zero visual or functional regressions.
- The HR Emploc Web module (OHM2026_1094) has been fully implemented in the frontend. The blank `/hr-emploc` shell was refactored into a high-fidelity read-only operational compliance queue using the shared AdminPageHeader, MetricCard, AdminFilterBar, DataState, StatusBadge, DetailDrawer, and CapabilityActionBar components. The client queries (`getWebHrEmplocSummary`, `listWebHrEmplocs`, and `getWebHrEmplocDetail`) are successfully wired to dynamic status tabs, search & filters (assignment, position, accountId, groupId), a compact dense grid table, and a 640px details drawer command center displaying placement parameter metrics, SLA breach alert warnings (Tiers 1 & 2), deficiencies checklist, corrected file attachments, separation/deletion banners, timeline logs, and permission badges. Verified successfully via pnpm lint and pnpm build (zero warnings, zero errors).
- Remaining gaps are backend implementation/mutations integration (Phase 4 action RPCs).
- The Plantilla Web module (OHM2026_1095) target architecture and state design are now fully defined and documented in `docs/state/plantilla_web_state.md`. This details Employee/Store dual views, scoped RBAC visibility, strict database-level PII masking rules, deactivation row styling, separation/backout clearance workflows, Additional Headcount (AH) temporary slot integrations, 640px sliding detail drawer layout, SLA/status breach timers, recommended SQL RPC contracts, and shared UI component mappings.
- The Plantilla Web query contract layer (OHM2026_1102) is implemented in `src/lib/queries/plantilla.ts`. Exported typed wrappers: `getPlantillaSummary`, `listWebPlantillaEmployees`, `listWebPlantillaStoreStaffing`, `getWebPlantillaDetail`. Full TypeScript types for employee roster, store staffing, summary metrics, detail record, row capabilities, deactivation overlay, transfer overlay, masked fields, and staffing risk. JSONB sub-normalizers for covered stores, government IDs, clearance checklist/documents, movement requests, and audit timeline. Presentation-layer derivation helpers: `deriveDeactivationOverlay`, `deriveTransferOverlay`, `deriveStaffingRisk`. Fail-closed `PlantillaDataError` with `access_denied`/`retryable` discriminator. Validated via `pnpm lint` and `pnpm build` (zero errors, zero warnings).
- The Plantilla Web module (OHM2026_1103) read-only dual-view shell is fully implemented in `src/app/(dashboard)/plantilla/page.tsx`. The blank `ModuleEmptyState` scaffold is replaced with a high-fidelity admin page using `AdminPageHeader` (readOnly badge), 4x `MetricCard` KPI row wired to `getPlantillaSummary`, `AdminFilterBar` with Employee/Store segmented view toggle, and two dense inline tables (employee roster, store staffing). Both tables use design-token classes (`table-header`, `table-row-hover`, `table-text`, `mono-pill-*`) consistent with Vacancy and HR Emploc. Employee view filters: search, account ID, group ID (UI only, pending RPC support), store ID, employment status, deployment type. Store view filters: search, account ID, group ID, staffing risk/SLA status. Deactivation overlays applied via `deriveDeactivationOverlay` (pending-separation: dashed border/bg-red-50; inactive/terminated: opacity-65/strikethrough). Staffing SLA breach indicators via `deriveStaffingRisk`. All four `DataState` boundaries (loading, empty, access_denied, retryable error with retry callback) are active for both list queries and summary metrics. Backend RPC migrations `20260607000002` and `20260607000003` are applied remotely; the frontend read-only shell is wired to those contracts. Remaining gaps: action mutations (Phase 5), group ID RPC parameter, store staffing detail drawer. Validated: `pnpm lint` clean, `pnpm build` clean (Next.js 16.2.4, zero errors).
- The Plantilla Detail Drawer (OHM2026_1104) is fully implemented and wired. `src/components/plantilla/PlantillaDetailDrawer.tsx` renders employment/allocation grid, roving coverage, clearance checklist (pending-separation only), SLA warning block, masked PII fields, audit timeline, and CapabilityActionBar. `getWebPlantillaDetail` is fetched via React Query on row selection. All four DataState boundaries (loading, access_denied, not found, retryable error) are active. Employee rows in the table are keyboard- and mouse-clickable (role=button, tabIndex=0, Enter/Space); dimmed rows (inactive/terminated/suspended) retain pointer-events-none and cannot open the drawer. Drawer close clears selectedPlantillaId state. Store staffing rows remain read-only with no drawer. Validated: `pnpm lint` clean, `pnpm build` clean (zero errors, zero warnings).
- A read workflow consistency audit (OHM2026_1105) has been applied across the Vacancy, HR Emploc, and Plantilla modules. Fixed: Plantilla page container padding/height, MetricCard prop names (canonical `isLoading`/`isBlocked`/`isError`/`badgeLabel`/`helperText`/`errorText`), KPI grid class, view toggle ARIA (role="tablist", aria-selected); HR Emploc `text-gray-550` non-existent class replaced with `text-gray-500`/`text-gray-600`, tablist `aria-label` added; Vacancy tablist `aria-label` added, Saved View button `cursor-not-allowed` added; DetailDrawer `role="dialog"` now has `aria-label`; VacancyDetailDrawer state icon containers aligned with HR Emploc (`shadow-xs border`). Validated: `pnpm lint` clean, `pnpm build` clean (zero errors, zero warnings).
- A system-wide UX, layout, spacing, density, and consistency audit (OHM2026_1096) has been performed and documented in `ux_consistency_audit.md`. This comprehensive review details the shared UI system quality, critical design token/Tailwind v4 gaps, visual differences between Vacancy and HR Emploc modules, drawer slide-in transition bugs, scanning density optimization, dark mode failures, and sidebar desktop dimension mismatches. An actionable, multi-phase technical integration blueprint is provided to bridge these gaps.
- The design token system (OHM2026_1097) is now activated in `src/app/globals.css`. It defines semantic surface/text/border tokens, a brand ramp, status semantic colors, spacing, drawer/sidebar widths, a z-index layer scale, and animation timing tokens. Semantic colors are runtime `:root` variables exposed via `@theme inline` and remapped under `prefers-color-scheme: dark`, giving native dark-mode support; static scales and animations use `@theme`; layout/z-index/spacing constants are plain `:root` vars consumed through arbitrary values. The shared primitives (`AdminPageHeader`, `MetricCard`, `DataState`, `DetailDrawer`, `AdminFilterBar`, `StatusBadge`) were refactored onto these tokens. Fixed: sidebar width mismatch (now `--sidebar-width` 240px, was `w-72`/288px), the `DetailDrawer` mount transition glitch (now uses the `animate-drawer-in` entrance keyframe and `--surface-overlay`/fade-in backdrop), HR Emploc row highlight/hover drift (standardized to `bg-blue-50` / `hover:bg-gray-50` like Vacancy), and invalid `gray-150`/`gray-850` classes. Drawer widths use `--drawer-width-md` (Vacancy 460px) and `--drawer-width-lg` (HR Emploc 640px). No module redesign, mutations, or new libraries were introduced. Verified via `pnpm lint` and `pnpm build` (clean).
- The first Web mutation workflow is architected (OHM2026_1107) and documented in `docs/state/web_mutation_workflow_state.md`. **HR Emploc deficiency tagging** (`Pending`/`For Review` → `For Correction`) was selected as the safest first mutation. The doc defines the full backend-authoritative RPC contract and strict phase order.
- The HR Emploc deficiency tagging mutation is **frontend-wired** (OHM2026_1109). Files changed: `src/lib/queries/hr_emploc.ts` (added `tagWebHrEmplocDeficiency` RPC wrapper, `TagDeficiencyParams`/`TagDeficiencyResult` types, `invalid_state` error kind for `P0001`); `src/components/shared/CapabilityActionBar.tsx` (buttons enabled when `isAvailable && !!onClick`); `src/components/hr-emploc/HrEmplocDetailDrawer.tsx` (`isTagModalOpen` state, canTag guard enabling the action button, `TagDeficiencyModal` sub-component with issue checkboxes + per-issue notes + remarks textarea + `useMutation` + query invalidation). On success: invalidates `["hr-emploc-detail", id]`, `["hr-emploc-list"]`, `["hr-emploc-summary"]`. Error handling: `42501` → access denied; `P0001` → backend validation message; generic → retryable. No optimistic UI. Validated: `pnpm lint` clean, `pnpm build` clean. **Backend deployed:** migration `20260607000004` applies `public.tag_web_hr_emploc_deficiency(...)` remotely. Mutation executes end-to-end.
- The **second** Web mutation workflow is architected (OHM2026_1110) and **frontend-wired** (OHM2026_1112). Documented in `docs/state/web_mutation_workflow_state.md` Part II (§10–§19): **HR Emploc correction review** (`For Review` → `Complete` on approve / `For Review` → `For Correction` on return). `reviewWebHrEmplocCorrection` RPC wrapper added to `src/lib/queries/hr_emploc.ts`. `canReviewCorrection` added to `HrEmplocRowCapabilities` (normalized from `can_review_correction | can_approve_correction`). `ReviewCorrectionModal` wired in `HrEmplocDetailDrawer.tsx`: per-deficiency Resolved/Still-Deficient toggles, auto-derived decision (all resolved → approve; any residual → return), decision-preview block, remarks textarea, submitting state, inline error display. On success: invalidates `["hr-emploc-detail", id]`, `["hr-emploc-list"]`, `["hr-emploc-summary"]`. No optimistic UI. Error handling: `42501` → access denied; `P0001` → backend validation; generic → network fallback. Validated: `pnpm lint` clean, `pnpm build` clean. **Backend deployed:** migration `20260609000000` applies `public.review_web_hr_emploc_correction(...)` remotely. **Remaining gap:** `can_review_correction` must be present in the `get_web_hr_emploc_detail` row capability payload (OHM2026_1110-IMPL-1).
- Dense table interiors tokenized (OHM2026_1098): `VacancyTable.tsx` and `HrEmplocTable.tsx` now use 12 dedicated table tokens (`table-header`, `table-row`, `table-row-hover`, `table-row-selected`, `table-rule`, `table-rule-section`, `table-text`, `table-text-sub`, `table-text-muted`, `mono-pill-surface`, `mono-pill-ink`, `mono-pill-ring`) replacing all hardcoded `bg-white`/`bg-gray-*`/`border-gray-*`/`text-gray-*`/selected/hover classes. Both tables are dark-mode readable. Existing columns, row click behavior, pagination, loading/empty/error states, and capability indicators are preserved. Validated via `pnpm lint` and `pnpm build` (clean).
